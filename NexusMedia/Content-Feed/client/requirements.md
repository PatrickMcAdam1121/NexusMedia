## Packages
framer-motion | Page transitions and scroll-triggered animations
date-fns | Human-readable date formatting

## Notes
Using Replit Auth for authentication (/api/login, /api/logout, /api/auth/user)
API paths are defined in @shared/routes
